interface:
  display_name: "Backend Linear Planner"
  short_description: "Plan backend implementation into Linear-sized work items"
  default_prompt: "Use $backend-linear-planner to read the AirHealth PRD, software architecture spec, and backend feature design, then break the work into epic, story, and task Linear tickets with each task sized for a reviewable sub-1000-line PR."
