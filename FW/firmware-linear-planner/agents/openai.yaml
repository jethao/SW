interface:
  display_name: "Firmware Linear Planner"
  short_description: "Plan firmware implementation into Linear-sized work items"
  default_prompt: "Use $firmware-linear-planner to read the AirHealth PRD, software architecture spec, and firmware feature design, then break the work into epic, story, and task Linear tickets with tasks small enough for a sub-1000-line PR."
