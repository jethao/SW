interface:
  display_name: "Firmware Linear Planner"
  short_description: "Plan firmware implementation into Linear-sized work items"
  default_prompt: "Use $firmware-linear-planner to gather the AirHealth PRD, software architecture spec, firmware feature design, and any needed existing Linear metadata before making Linear calls, then break the work into epic, story, and task tickets with each task sized for a reviewable sub-1000-line PR."
