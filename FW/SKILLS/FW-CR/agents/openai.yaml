interface:
  display_name: "FW CR"
  short_description: "Review firmware PRs against linked FIR tickets"
  default_prompt: "Use $fw-cr to review open firmware PRs in jethao/SW, compare each PR against its linked FIR Linear ticket, request changes for mismatches or correctness issues, and approve only when the implementation looks right."
