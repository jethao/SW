interface:
  display_name: "APP ENG"
  short_description: "Implement ready APP backlog tickets and request Codex PR review"
  default_prompt: "Use $app-eng to gather Linear ticket, dependency, PR, comment, and local app context before making API writes, then select only unblocked ready APP tickets, implement them under SW/APP, create one linked pull request per completed ticket, and request Codex review on the GitHub PR."
