interface:
  display_name: "FW ENG"
  short_description: "Implement ready FIR backlog tickets and open one PR per ticket"
  default_prompt: "Use $fw-eng to gather Linear ticket, dependency, PR, comment, and local firmware context before making API writes, then select only unblocked ready FIR tickets, implement them under SW/FW/Source, and create one linked pull request per completed ticket."
